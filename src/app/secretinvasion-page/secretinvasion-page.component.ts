import { Component } from '@angular/core';

/**
 * Component representing the Secret Invasion page.
 *
 * The `SecretinvasionPageComponent` is responsible for displaying information about the "Secret Invasion" TV show,
 * including an overview, promotional banner, and embedded video. It utilizes the component's template and styles 
 * defined in the associated HTML and CSS files.
 */
@Component({
  selector: 'app-secretinvasion-page', // The HTML tag for this component
  templateUrl: './secretinvasion-page.component.html', // Path to the component's HTML template
  styleUrls: ['./secretinvasion-page.component.css'] // Path to the component's CSS styles
})
export class SecretinvasionPageComponent {
  // The component class is currently empty but can be extended with properties and methods
  // to handle business logic, data binding, and interactions in the future.
}
