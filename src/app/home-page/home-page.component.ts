import { Component } from '@angular/core';

@Component({
  selector: 'app-home-page', // Selector used to include this component in templates
  templateUrl: './home-page.component.html', // Path to the component's HTML template
  styleUrls: ['./home-page.component.css'] // Path to the component's CSS styles
})
export class HomePageComponent {
  // This component currently does not have any properties or methods
  // It serves as the base for the Home page content, with the structure defined in the HTML template
}
