import { Component } from '@angular/core';

@Component({
  selector: 'app-doctor-strange-page', // Selector used to include this component in templates
  templateUrl: './doctor-strange-page.component.html', // Path to the component's HTML template
  styleUrls: ['./doctor-strange-page.component.css'] // Path to the component's CSS styles
})
export class DoctorStrangePageComponent {
  // This component currently does not have any properties or methods
  // It serves as the base for the Doctor Strange page content, with the structure defined in the HTML template
}
