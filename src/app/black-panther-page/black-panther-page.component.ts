import { Component } from '@angular/core';

@Component({
  selector: 'app-black-panther-page', // Selector used to include this component in templates
  templateUrl: './black-panther-page.component.html', // Path to the component's HTML template
  styleUrls: ['./black-panther-page.component.css'] // Path to the component's CSS styles
})
export class BlackPantherPageComponent {
  // This component currently does not have any properties or methods
  // It serves as the base for the Black Panther page content, with the structure defined in the HTML template
}
