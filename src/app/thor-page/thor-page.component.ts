import { Component } from '@angular/core';

// Component decorator to define metadata for the ThorPageComponent
@Component({
  selector: 'app-thor-page', // The selector that identifies this component in HTML templates
  templateUrl: './thor-page.component.html', // Path to the HTML template for the component
  styleUrls: ['./thor-page.component.css'] // Path to the CSS styles for the component
})
export class ThorPageComponent {
  // The component class is currently empty, but this is where
  // any properties, methods, or lifecycle hooks would be added in the future
}
