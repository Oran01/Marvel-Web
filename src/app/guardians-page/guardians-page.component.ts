import { Component } from '@angular/core';

@Component({
  selector: 'app-guardians-page', // Selector used to include this component in templates
  templateUrl: './guardians-page.component.html', // Path to the component's HTML template
  styleUrls: ['./guardians-page.component.css'] // Path to the component's CSS styles
})
export class GuardiansPageComponent {
  // This component currently does not have any properties or methods
  // It serves as the base for the Guardians of the Galaxy page content, with the structure defined in the HTML template
}
