import { Component } from '@angular/core';

@Component({
  selector: 'app-ant-man-page', // Selector used to include this component in templates
  templateUrl: './ant-man-page.component.html', // Path to the component's HTML template
  styleUrls: ['./ant-man-page.component.css'] // Path to the component's CSS styles
})
export class AntManPageComponent {
  // This component currently does not have any properties or methods
  // It serves as the base for the Ant-Man page content, with the structure defined in the HTML template
}
