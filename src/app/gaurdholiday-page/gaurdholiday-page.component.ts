import { Component } from '@angular/core';

@Component({
  selector: 'app-gaurdholiday-page', // Selector used to include this component in templates
  templateUrl: './gaurdholiday-page.component.html', // Path to the component's HTML template
  styleUrls: ['./gaurdholiday-page.component.css'] // Path to the component's CSS styles
})
export class GaurdholidayPageComponent {
  // This component currently does not have any properties or methods
  // It serves as the base for the Gaurdholiday page content, with the structure defined in the HTML template
}
